# 像素英语小镇：Agent 操作手册

本文件是本仓库内所有开发、设计、素材生成与页面调整的长期约束。用户的当次明确要求优先于本文件；若用户未明确要求，则必须遵守本文件。

## 1. 工作范围与变更纪律

- 只修改用户明确点名的页面、功能、文案或素材。
- 不得以“顺手优化”为由改动其他页面、认证、密码规则、数据库、接口、COS、布局或视觉。
- 若目标改动必然会影响其他页面或共享组件，必须先说明影响范围与原因，等待确认后再改。
- 恢复旧版时，优先从本地源码、构建缓存或版本历史找回准确实现；不得凭印象重做布局、文案或坐标。
- 每次完成后，说明实际改动的文件和未改动的范围。

## 2. 当前登录页约定

- 登录场景背景使用 `public/images/come-background-v1.png`。
- 未登录点击“点击进入小镇”后：场景上滑并停在顶部，下方页面变为完整登录/注册输入区。
- 已登录用户点击入口后直接进入 `/town`。
- 首页文案使用：
  - `PIXEL ENGLISH TOWN`
  - 游客：`在小镇里，开始今天的英语冒险`
  - 已登录：`欢迎回到小镇，{用户名}`
  - `阅读、练习、收集徽章。学习广场已经亮起灯火，等你加入。`
- 入口按钮独立于背景图片，可单独点击；位置、大小和文案未经用户要求不得调整。

### 首页响应式排版（强制）

- 游客文案固定为：`PIXEL ENGLISH TOWN`、`在小镇里，开始今天的英语冒险`、`阅读、练习、收集徽章。学习广场已经亮起灯火，等你加入。`。
- 已登录文案固定为：`欢迎回到小镇，{用户名}`、`阅读、练习、收集徽章。学习广场已经亮起灯火，等你加入。`；不显示游客标签。
- 首页文字和按钮距左右屏幕边缘至少 `5%`，距画面底边至少 `12%`。
- 文案容器必须设置最大宽度；窄屏时字体使用响应式缩放，不得与按钮、背景素材或屏幕边缘挤压、重叠或被裁切。
- 文案区和按钮区必须分别预留高度，禁止依赖固定像素凑位置。

## 3. 像素视觉规范

- 统一使用“像素英语小镇”的明亮、清新、卡通像素风：硬边、粗深色像素轮廓、清晰块面、马卡龙配色。
- 地图页面采用分层渲染：底层草地/道路背景 + 独立透明建筑/装饰 DOM。建筑必须可独立点击、悬浮、显隐和添加动画。
- UI 组件、建筑与装饰应为彼此分离的素材或 DOM，不得融合粘连；组件之间保留清晰间距。
- 非开放建筑只可灰显并提示“建设中”，不得跳转到空白页或伪功能页面。
- 移动端根据空间改为抽屉或底部面板，不强行压缩三栏布局。

## 4. 绝对禁止的视觉元素与提示词

页面、素材生成提示词、图片编辑提示词和代码样式中，禁止出现或引入以下内容：

- 复杂光影
- 写实
- 模糊抗锯齿
- 组件互相重叠
- 背景杂乱
- 人物遮挡
- 3D
- 照片质感
- 大量渐变阴影
- 星露谷物语
- 牧场物语
- RPG游戏地图
- 瓦片图块
- 丰富渐变光影
- 噪点
- 复杂材质
- 抗锯齿模糊边缘
- 景深
- 写实油画质感
- 大量细小像素
- 柔和轮廓
- 泥土石头纹理
- 溪流光影
- 黄昏环境光
- 厚涂
- 复杂景深

如用户提供的参考图或请求与上述禁项冲突，应先指出冲突并请求确认，不得自行折中加入禁项。

## 5. 素材与前端实现

- 新生成或编辑的项目素材须同时保存到项目可追踪的素材目录，并在被页面引用前完成视觉检查。
- 不覆盖用户提供的原始图片；派生素材使用版本化文件名，例如 `*-v2.png`。
- 透明素材须检查透明边缘、无底色方块、无文字残留、无锯齿模糊。
- 图片仅负责像素场景与装饰；用户名、欢迎语、动态数字、通知、按钮状态等必须由前端代码渲染。
- 不得为了视觉修改认证规则、密码长度、数据库结构或 API 行为。

## 6. 完成前检查

- 确认目标路由可访问，且未登录/已登录两种状态符合要求。
- 确认没有新增重复按钮、重复文案、遮罩或未经要求的全屏/全宽布局。
- 确认本次没有改动未授权页面；如共享代码产生影响，必须在交付中明确说明。
